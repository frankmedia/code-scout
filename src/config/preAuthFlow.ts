/**
 * Bump this string whenever pre-sign-in screens change. On first load after a bump,
 * `codescout_pre_auth_done_*` is cleared so the two-step guide shows again (once you are signed out).
 */
export const PRE_AUTH_FLOW_VERSION = '8';
